
function grow!(node::RuleNode)
    # sample which node
    # get the deepness that that node :/
    # do the thing
end 

function prune!(node::RuleNode)
end 

function delete!(node::RuleNode)
end 

function insert_node!(node::RuleNode)
end 

function re_operator!(node::RuleNode)
end 

function re_feature!(node::RuleNode)
end 

#node_index = sample(NodeLoc, tree)
#old_node = get(tree, node_index)
#new_node = rand(RuleNode, grammar, :Real, 3)
#insert!(tree, node_index, new_node)
#new_equation = get_executable(tree, grammar)
